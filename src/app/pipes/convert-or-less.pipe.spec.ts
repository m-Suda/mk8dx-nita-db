import { ConvertOrLessPipe } from './convert-or-less.pipe';

describe('ConvertOrLessPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertOrLessPipe();
    expect(pipe).toBeTruthy();
  });
});
