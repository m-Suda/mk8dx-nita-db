import { CalcTimeDiffPipe } from './calc-time-diff.pipe';

describe('CalcTimeDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcTimeDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
